# Criar uma classe Musica, que deverá receber como parâmetro de criação uma lista
# de string com as estrofes de uma música. Esta classe deverá conter um método
# chamado cante_para_mim que imprima todas as estrofes, uma em cada linha.
# Para executar, instancie a classe e execute o método.

class Música:

    def cante_para_mim(self):
        lista_estrofes = []
        lista_estrofes.append(self)
        print(lista_estrofes)
